makeEmpty(BinaryNode* & t) Analysis:

This function has a O(n) runtime complexity as it must recursively run for each node in the binary tree. So if there are 100 elements in the tree, this function will run 100 times.